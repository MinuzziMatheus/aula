services:
  db:
    environment:
      POSTGRES_DB: finance_api_local
    ports:
      - "5432:5432"

  app:
    environment:
      NODE_ENV: local
      DB_NAME: finance_api_local
    ports:
      - "3000:3000"
