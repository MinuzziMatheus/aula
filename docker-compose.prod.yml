services:
  db:
    environment:
      POSTGRES_DB: finance_api_prod
    ports:
      - "5434:5432"

  app:
    environment:
      NODE_ENV: production
      DB_NAME: finance_api_prod
    ports:
      - "3002:3000"
